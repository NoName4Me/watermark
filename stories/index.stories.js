import React from 'react';
import Normal from './svg-canvas';
import Random from './dom';

export default {
  title: 'watermark',
};

export const svgCanvas = () => <Random />;

export const useDom = () => <Normal />;
